<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Sweet Music</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>🎵 Sweet Music</h1>
    <form method="get">
      <input name="q" placeholder="Пошук...">
      <button type="submit">🔍</button>
      <a href="/upload">➕ Завантажити</a>
    </form>
    <ul>
      <% tracks.forEach(track => { %>
        <li>
          <h3><%= track.title %> — <%= track.artist %> (<%= track.plays %> прослух.)</h3>
          <% if (track.cover) { %>
            <img class="cover" src="<%= track.cover %>" alt="cover">
          <% } %>
          <audio controls onplay="fetch('/play/<%= track.id %>', {method:'POST'})">
            <source src="<%= track.music %>">
          </audio>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>