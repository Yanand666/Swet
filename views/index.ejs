<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Sweet — Music</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <header class="site-header">
    <div class="logo">Sweet</div>
    <nav class="main-nav">
      <a href="#">Головна</a>
      <a href="#">Тренди</a>
      <a href="#">Виконавці</a>
      <a href="#">Завантажити</a>
      <a href="#">Мій плейлист</a>
    </nav>
    <div class="search-wrap">
      <input id="search" placeholder="Пошук треків, виконавців..." />
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <h1>Ласкаво просимо в <span class="accent">Sweet</span></h1>
      <p class="sub">Темна неонова платформа для анонімного завантаження та прослуховування музики.</p>
      <div class="cta-row">
        <button id="uploadBtn" class="btn">Завантажити трек</button>
      </div>
    </section>

    <section class="trending">
      <h2>Зараз у тренді</h2>
      <div id="tracks" class="tracks-grid">
        <!-- Track cards inserted by JS -->
      </div>
    </section>

    <section class="upload-section" id="uploadSection" aria-hidden="true">
      <h2>Завантажити трек</h2>
      <form id="uploadForm">
        <label>Назва треку<input name="title" required /></label>
        <label>Виконавець<input name="artist" required /></label>
        <label class="file-label">Файл (мп3, wav)
          <input id="fileInput" name="file" type="file" accept="audio/*" required />
        </label>
        <div class="form-actions">
          <button type="submit" class="btn primary">Завантажити</button>
          <button type="button" id="cancelUpload" class="btn">Скасувати</button>
        </div>
        <p class="hint">Це демо: файли зберігаються лише локально в браузері (FormData). Для збереження на сервері потрібен бекенд.</p>
      </form>
    </section>

  </main>

  <footer class="site-footer">
    <div>© <span id="year"></span> Sweet — All rights reserved</div>
    <div class="small">Built with ❤️ — Replace assets in <code>/assets</code></div>
  </footer>

  <!-- Floating audio player -->
  <div class="player" id="player">
    <div class="player-info">
      <img id="playerCover" class="cover" src="" alt="" />
      <div>
        <div id="playerTitle">Нічого не грає</div>
        <div id="playerArtist" class="small muted"></div>
      </div>
    </div>
    <div class="player-controls">
      <button id="prevBtn" class="ctrl">⏮</button>
      <button id="playBtn" class="ctrl">▶️</button>
      <button id="nextBtn" class="ctrl">⏭</button>
      <div class="time">
        <span id="currentTime">0:00</span> / <span id="duration">0:00</span>
      </div>
    </div>
  </div>

  <audio id="audio" preload="metadata"></audio>

  <script src="app.js"></script>
</body>
</html>
