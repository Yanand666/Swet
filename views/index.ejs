
<!DOCTYPE html>
<html>
<head>
  <title>Sweet Music</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <div class="container">
    <h1>🎵 Sweet Music</h1>
    <form method="get" action="/">
      <input type="text" name="q" placeholder="Пошук за назвою або виконавцем">
      <button type="submit">🔍</button>
    </form>
    <a href="/upload">⬆️ Завантажити трек</a>
    <div class="track-list">
      <% tracks.forEach(track => { %>
        <div class="track">
          <% if (track.cover) { %>
            <img src="<%= track.cover %>" alt="Обкладинка">
          <% } %>
          <h3><%= track.title %> - <%= track.artist %></h3>
          <audio controls onplay="fetch('/play/<%= track.id %>', { method: 'POST' })">
            <source src="<%= track.music %>" type="audio/mpeg">
          </audio>
          <p>👁️ <%= track.plays %> прослуховувань</p>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html>
